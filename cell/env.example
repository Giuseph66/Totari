# Totari Mobile App Environment Variables

# Backend API URL (not used in Firebase version)
# BACKEND_BASE_URL=https://your-backend-url.com

# Application environment (dev|prod)
APP_ENV=dev

# ElevenLabs API Key (DEV ONLY - In production, use a proxy)
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Google Gemini API Key (DEV ONLY - In production, use a proxy)
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase configuration
# Note: Firebase config is in src/config/firebase.ts

# Security Notes:
# - Never store sensitive API keys (ELEVENLABS_API_KEY) in production client
# - All sensitive operations should be handled by a backend proxy in production
# - JWT tokens are stored securely using expo-secure-store
# - All communication with Firebase is via HTTPS